# MongoDB connection URI
MONGO_URI="mongodb+srv://gpsgauravgps:8650093027@trip.g2ps4ea.mongodb.net/tripforge?retryWrites=true&w=majority&appName=trip"

# JWT secret key (keep this secure in production)
JWT_SECRET=your_super_secret_jwt_key

# Node environment
NODE_ENV=development

# CORS settings (comma separated if needed)
ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# Port for the Node server
PORT=3000

# Any other secrets or configs can go here